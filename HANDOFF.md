# 인수인계 — 2026-08-08 (회사 PC → 집 PC)

> 이 파일이 존재하는 이유: Claude의 메모리는 PC 로컬에만 저장돼 기기 간에 따라오지 않습니다.
> 집 PC에서 이어가려면 이 파일이 유일한 맥락입니다. **먼저 `git pull` 하세요.**

## 오늘 무슨 일이 있었나

리본/아카이브 세계관을 **폐기**하고 채용 심사자 기준으로 다시 만들었습니다.

폐기한 이유(사용자 판단 + 실측):

- 심사자가 은유(소장 이력·커스터디 리본)를 해독할 이유가 없다
- 세리프 디스플레이(Caslon·고운바탕)가 "웹 개발자"가 아니라 "출판·아카이브"로 읽힌다
- 실측 결과 **텍스트 135개 중 52개가 실효 14px 미만**이었다(최악 7.2px).
  원인은 `font-variant-caps: all-small-caps` — 대문자 높이가 em의 약 72%라
  11px로 지정한 라벨이 실제로는 7.9px로 그려지고 있었다

시안 A~F를 목업으로 만들었으나 전부 폐기(사용자 평가: "다 구려"). `redesign-existing-projects`
스킬의 감사 목록이 그 시안들을 그대로 저격했습니다 — "밝은 페이지 중간의 어두운 섹션은
복붙 사고처럼 보인다", "테두리+그림자+흰 배경 제네릭 카드", "빈 채로 납작한 섹션".
그래서 목업을 접고 실제 코드에 스킬의 Fix Priority 순서대로 적용했습니다.

## 적용한 것

**1. 폰트** — Caslon·Caslon Display·고운바탕 3종 제거 → **Geist(라틴) + Pretendard(한글)** 2종.

**2. 색** — 액센트 **하나**(`#b4412a` 웜 러스트). 상태 표시(운영 중)도 초록을 새로 들이지 않고
같은 액센트를 씁니다. 회색은 웜 한 계열. 그림자는 배경 색조를 띠게(`rgba(36,30,24,…)`).

**3. 조판** — `.font-label`(스몰캡) **전면 폐기** → `.label`(13px/600). `globals.css`에 하한을 박았습니다:

```css
main :is(p, span, li, dt, dd, figcaption) { font-size: max(1em, 13px); }
main p:not([class*="max-w"]):not([class*="w-"]) { max-width: 68ch; }
```

**4. 레이아웃** — 하드코딩 좌우 패딩 → `.wrap`(최대폭 1280 + 반응형). 리본용 제본 여백 제거.

**5. 히어로 재작성** — 심사자가 15초에 읽는 순서:
이름·직무·소속 → 주장 한 줄 → **운영 중 2건(증거)** → 스택 → CTA.

**6. 프로젝트 목록 재작성**(`ProjectLedger.tsx`) — 카드 그리드·스티키 스택 폐기.
`why` 배열에서 *문제를 재정의한 문장*을 자동으로 골라(`keyReason()`) 액센트 규칙선과 함께
큰 글씨로 세웁니다. 스택은 항목마다 모노스페이스 한 줄.

**7. AI 섹션 정리** — `— Claude (Anthropic) — 함께 일하는 AI의 평가` **서명 삭제**.
문장이 전부 1인칭인데 AI 평가로 서명하면 화자를 알 수 없고, 자기 평가를 제3자 검증처럼
포장한 것으로 읽혀 PRODUCT.md의 "과장·미검증 클레임 금지"에 걸립니다.
`<blockquote>` → `<ul>`로 마크업도 교체(인용 서식은 화자가 따로 있다는 뜻).
2줄 삭제, 데이터 경계를 맨 위로, 주석에만 있던 오진단 정정 근거를 화면에 노출.
`assessment` → `principles` + `boundary`로 데이터 구조 변경.

**8. 상세 페이지 진입점 복구** — 목록 재작성 때 이름에만 링크가 남아 상세가 사라진 것처럼
보였습니다. `케이스 스터디 — 구조 · 문제 해결 · 결과 →` CTA + 도판 전체 링크로 복구.

## 실측 (Chrome 확장으로 DOM 직접 측정)

| 항목 | 리본 버전 | 현재 |
|---|---|---|
| 실효 13px 미만 | 52개 (최악 7.2px) | **0개** |
| 스몰캡 | 다수 | **0** |
| 최저 대비 | 4.74:1 | **5.21:1** |
| 본문 줄 길이 | 최대 103자 | 최대 90 · **중앙값 66자** |
| AA 미달 | 0 | 0 |
| 폰트 종류 | 4종 | **2종** |
| 모바일 390px 가로 넘침 | 미검증 | **0px** |

## 남은 일

1. **상세 페이지 조판** — 안 깨졌지만 `Why` 본문이 과하게 크고,
   `SYSTEM DESIGN & PRIVACY` 배지가 제목 오른쪽에 붕 떠 있습니다. 구조는 그대로 두고 조판만.
2. **나머지 프로젝트 4건의 `why`** — 목록에서 `keyReason()`이 "하지만/진짜 문제"가 있는
   문장을 찾는데, Robotics만 그 패턴이 있고 나머지는 첫 줄이 그냥 나옵니다. 문장을 다듬거나
   데이터에 `keyWhy` 필드를 두는 게 나을 수 있습니다.
3. **impeccable FINISH 계약 미이행** — 리본 버전에서 마감 리뷰·verdict까지 갔으나
   `DESIGN.md`(documenter)를 안 썼고, 그 뒤 세계관을 갈아엎어 리뷰 결과 자체가 무효가 됐습니다.
   새 세계로 다시 돌리려면 `.claude/skills/impeccable/reference/new-work.md` §6-7.
   `layout.tsx`의 방향 계약 주석은 **아직 리본 세계관 그대로라 현재 빌드와 불일치** — 고쳐야 합니다.
4. 접근성 critique의 P1 3건(히어로 증거 부족·reduced-motion 절반·카드 링크 60단어) 미해결.

## 작업 환경 메모

- 정적 미리보기: `npm run build` 후 `python -m http.server 4321 --bind 127.0.0.1 --directory out`
  (`--directory`를 빼면 `out/`이 잠겨 다음 빌드가 EBUSY로 실패합니다)
- 스크린샷: `--virtual-time-budget`은 **Lenis의 무한 rAF 루프에 예산을 다 뺏겨**
  애니메이션이 0.4초에서 멎은 화면만 찍힙니다. 실시간 대기가 되는 CDP 캡처를 쓰세요.
- framer-motion에서 SVG `transform-origin`은 CSS로 주면 안 됩니다 —
  framer가 자기 기본값(50% 50%)으로 덮어씁니다. `originX`/`originY` 프로퍼티로 주세요.
- 레이어 밖 CSS는 Tailwind 유틸리티를 이깁니다. `.thread-known`이 4면 `border-color`를
  잡아 `border-line`을 이기고 괘선을 전부 붉게 만든 적이 있습니다.
