# 인수인계 — 2026-08-15 (회사 PC → 집 PC)

> 이 파일이 존재하는 이유: Claude의 메모리는 PC 로컬에만 저장돼 기기 간에 따라오지 않습니다.
> 집 PC에서 이어가려면 이 파일이 유일한 맥락입니다. **먼저 `git pull` 하세요.**

## 지금 상태

08-08(회사) → 08-14(집, 9커밋) → 08-15(회사, 이 커밋). 08-08 인수인계에 적혀 있던
"남은 일" 4개는 **전부 닫혔습니다.** 아래 표가 그 대조입니다.

| 08-08의 남은 일 | 처리 |
|---|---|
| 1. 상세 페이지 조판(Why 본문 큼, 배지 뜸) | `de3c156`·`483d0d2` — 본문 단을 `--measure` 하나로 통일 |
| 2. 나머지 프로젝트 4건의 `why` | **이번 커밋** — 5건 전부 문제 재정의 문장을 갖게 됨 |
| 3. impeccable FINISH·DESIGN.md·layout 주석 불일치 | `ecca87c` — DESIGN.md 작성, 리본 주석 삭제 |
| 4. 접근성 P1 3건 | 히어로 증거·카드 링크는 08-14에, reduced-motion은 **이번 커밋** |

## 오늘(08-15) 한 것

**1. reduced-motion이 절반만 걸려 있던 것** — `globals.css`의 감소 블록은 animation·transition만
잡는데, Lenis는 rAF로 스크롤 위치를 직접 보간해서 CSS로는 꺼지지 않았습니다.
`SmoothScroll.tsx`에서 `(pointer: coarse), (prefers-reduced-motion: reduce)` 한 쿼리로
Lenis 인스턴스 생성 자체를 건너뜁니다.

**2. 프로젝트 CTA 링크 5개의 접근 이름이 전부 같았던 것** — 보이는 글자
("케이스 스터디 — 구조 · 문제 해결 · 결과 →")는 그대로 두고 `aria-label`에만 프로젝트명을
붙였습니다. 스크린리더 링크 목록에서 5개가 구분됩니다.

**3. `why` 5건 전면 재작성** — `keyReason()`이 "하지만/진짜 문제"가 든 문장을 골라
목록에 큰 글씨로 세우는데, 그 패턴이 있는 건 Robotics 하나뿐이었습니다. 나머지 4건은
`why[0]`이 그냥 나와서, VODA는 "Web-RTC 기술을 활용한 서비스라는 주제에서 출발했습니다"
— 과제 설명이 첫 줄이었습니다.

네 건 다 **문제 → 왜 → 무엇을 했나** 순서로 다시 썼습니다. 특히 VODA는 사용자 지시로
문제 정의를 **비언어 정보 결핍**으로 바꿨습니다 — 설리반+·Be My Eyes는 "무엇이 있는지"까지만
알려주고 상대의 표정은 전달하지 않는다는 것. 복지관 방문도 highlights에만 묻혀 있던 것을
랜딩으로 올렸습니다.

`restOfReason()`이 `slice(0, 2)`라 랜딩에는 key + 앞의 두 줄만 나옵니다. **배열 순서가
곧 랜딩 노출 여부**이므로, 랜딩에 띄우고 싶은 문장은 3번째 안에 두세요.

## 남은 일 — 전부 "숫자가 없다" 하나로 모입니다

포트폴리오를 "A 문제 → X 실행 → **정량 수치**만큼 개선"으로 바꾸기로 했는데,
재고 조사 결과 **쓸 수 있는 숫자가 사실상 VODA의 명도 대비 7.86 하나**였습니다.

| 프로젝트 | 있는 수치 | 상태 |
|---|---|---|
| VODA | 명도 대비 7.86 (WCAG AA 4.5의 1.7배) | ✅ 자소서 마스터에도 있는 검증값 |
| Open The Door | 개발 시간 약 50% 단축 | ⚠️ "무엇 대비 50%"가 없음 |
| Open The Door | `.map()`→FlatList, 10개 페이지네이션 | ⚠️ 커밋으로 확인. **개선 전후 수치 없음** |
| HARUMAN | 7주 기한 | 기간이지 개선치가 아님 |
| RAIM | 기획~배포 2주 / 장애 롤백 34분 | ✅ 검증됨(속도·대응이지 개선은 아님) |
| Robotics | 테이블 10석 · 입장기한 11분 | 설계값이지 결과가 아님 |

`results` 필드도 5건 전부 **수상 실적이거나 "운영 중"** — 결과가 아니라 상태입니다.
숫자가 들어오면 여기를 "무엇이 얼마나 달라졌는지"로 바꾸는 게 다음 작업입니다.

**본인만 가져올 수 있는 것** (값이 큰 순서, 하나만 있어도 Robotics 항목이 달라집니다):

1. Robotics — 하루 평균 대기 팀 수, 누적 운영일, 누적 방문객 수 (직원 대시보드 엑셀 내려받기)
2. Robotics — 도입 전후 호출 누락 건수. 체감이라도 좋음 — "거의 매일" → "0"이면 그게 결과
3. RAIM — 쓰는 직원 수, 크롤러가 자동 수집한 연간 교육 건수(수기로 옮겨 적던 것 대비)
4. VODA — **복지관에서 받은 피드백의 구체 내용**. 지금은 "받아서 반영했다"까지만 있어
   무엇을 고쳤는지가 비어 있습니다
5. Open The Door — 50%가 무엇 대비인지 (직전 프로젝트 같은 규모 화면? 팀 추산?)

> ⚠️ **운영 DB에서 뽑으려 하지 마세요.** `Desktop\reservation\backend\db.sqlite3`는
> 개발 사본이고, `dailystat` 10행이 전부 테스트 데이터입니다(2026-07-08 캡처 작업일 행 포함,
> `total_count`와 성별 합이 안 맞는 행도 있음). `visitor`는 0건 — 자동 파기 설계대로입니다.
> **진짜 숫자는 관내 내부망 서버에만 있습니다.**

## 작업 환경 메모 (계속 유효)

- 정적 미리보기: `npm run build` 후 `python -m http.server 4321 --bind 127.0.0.1 --directory out`
  (`--directory`를 빼면 `out/`이 잠겨 다음 빌드가 EBUSY로 실패합니다)
- 스크린샷: `--virtual-time-budget`은 **Lenis의 무한 rAF 루프에 예산을 다 뺏겨**
  애니메이션이 0.4초에서 멎은 화면만 찍힙니다. 실시간 대기가 되는 CDP 캡처를 쓰세요.
- claude-in-chrome 탭은 `visibility:hidden`이라 rAF·IntersectionObserver가 멈춥니다.
  스크롤 애니메이션이나 lazy 로딩이 "0/미발화"로 나와도 실버그로 오진하지 마세요.
- framer-motion에서 SVG `transform-origin`은 CSS로 주면 안 됩니다 —
  framer가 자기 기본값(50% 50%)으로 덮어씁니다. `originX`/`originY` 프로퍼티로 주세요.
- 레이어 밖 CSS는 Tailwind 유틸리티를 이깁니다.
- 한글을 새로 쓰면 `scripts/check-font-coverage.mjs`가 잡습니다 — 폰트 CSS가 빌드 시
  실제 쓰는 글자만 남기고 잘리므로, 빠진 글자는 조용히 시스템 폰트로 떨어집니다.
  배포 워크플로가 업로드 전에 돌리지만, 로컬에서도 빌드 후 한 번 돌려보세요.
- 지원 직무별 URL: 개발이면 `/`, 기술영업이면 `/field` (README 표 참조).
