# DESIGN.md

이 사이트가 지금 실제로 쓰는 값과, 그 값을 그렇게 정한 이유. **추정이 아니라 코드에서 읽은 값**이다.
`PRODUCT.md`가 무엇을 말할지를 정하고, 이 문서는 어떻게 보일지를 정한다.

> ⚠️ 2026-08-15 작성. `layout.tsx`에 HTML 주석으로 박혀 있던 "붉은 리본 / 버프지" 방향 계약을
> 대체한다. 그 세계관은 커밋 `70fc6ae`에서 폐기됐는데 주석만 페이지마다 남아 있었다.

---

## 1. 이 화면이 하는 일

심사자가 **15초 안에** 이 순서로 읽게 만든다.

1. 누구인가 → 2. 무엇을 주장하는가 → 3. **그 증거가 지금 돌아가는가** → 4. 무엇으로 만들었는가

여기서 이탈해도 "지금 근무지에서 운영 중 · 2건"은 읽고 나가야 한다.
그래서 Hero는 주장 바로 다음에 실운영 2건을 놓고, 그 옆에 실제 대시보드 화면을 건다.

**보는 사람**: 채용 담당자·개발 팀장. 개발자 동료가 아니다.
**모드**: `Read` — 안정성·스캔성·측정이 먼저다. 화려함이 아니다.

---

## 2. 색

한 계열(따뜻한 중성)만 쓴다. 회색을 웜·쿨로 섞으면 화면이 탁해진다.
**액센트는 하나뿐이다.** 상태 표시(운영 중)도 초록을 새로 들이지 않고 이 액센트를 쓴다.

| 토큰 | 값 | 용도 | 대비 (vs `--background`) |
|---|---|---|---|
| `--background` | `#f7f6f3` | 바탕 | — |
| `--background-elevated` | `#efeee9` | 이미지 플레이트 바닥 | — |
| `--foreground` | `#17181c` | 본문·제목 | **16.1:1** |
| `--foreground-dim` | `#4a4c54` | 부연 문단 | **7.7:1** |
| `--foreground-mute` | `#63656e` | 라벨·캡션 | **5.2:1** |
| `--accent` | `#b4412a` | 링크·강조·운영 중 | **5.1:1** |
| `--accent-soft` | `#f2e5e0` | 액센트 헤일로 | — |
| `--line` | `#dedcd5` | 1px 괘선 | — |

**전부 WCAG AA 통과.** `PRODUCT.md`의 절대 제약이다.

🔴 **프로젝트마다 다른 파스텔을 쓰지 않는다.** 리디자인 전 5종 파스텔은 이 바탕 위에서
1.4~2.36:1까지 떨어져 AA를 깼다. **색이 아니라 괘선이 구분한다.**
🔴 본문은 순수 검정이 아니다. `#000`은 종이 위에서 때처럼 보인다.

---

## 3. 타이포그래피

### 서체

| 역할 | 서체 | weight |
|---|---|---|
| 라틴 디스플레이·본문 | **Geist** (`next/font`) | 400·500·600·700·800 |
| 한글 | **Pretendard** (셀프호스팅 서브셋) | 400·500·600·700 |
| 숫자·코드·스택 표기 | **Geist Mono** | 400·500 |

**세리프를 걷어낸 자리다.** 세리프 디스플레이는 "웹 개발자"가 아니라 "아카이브·출판"으로 읽힌다.

⛔ **서울한강체·서울남산체 검토했고 채택하지 않았다** (2026-08-15, 브라우저 실측).
서울서체는 L/M/B/EB **4종뿐**인데 이 사이트는 다섯 단계를 쓴다 → 500이 M(=400)과,
700이 B(=600)와 같아져 **위계가 5단계에서 3단계로 눌린다.** 그리고 인쇄·사인 시스템용이라
13~17px 화면 힌팅이 Pretendard(화면 UI 전용)만 못하다.
한강체 M 단독은 `font-extrabold` 제목이 400으로 그려져 헤드라인이 죽고, 본문만 바꾸면
600 이상이 Pretendard로 떨어져 **한 문단 안에 두 서체가 섞인다.**

### 역할 스케일

| 역할 | 값 | 쓰는 곳 |
|---|---|---|
| **Display** | `.font-display` — tracking `-0.035em`, line-height `1.08` | h1·h2·프로젝트명 |
| **Hero** | `clamp(2.4rem, 5.4vw, 4.1rem)` / 800 | 랜딩 h1 |
| **Lead** | 20~30px / 500~600 | 상세 Why, ConceptStrip |
| **Body** | **17px / 1.65** / 400 | `p, li, dt, dd, figcaption` |
| **Label** | **13px / 600 / +0.02em / 1.5** / `--foreground-mute` | `.label` |
| **Mono** | 13px | 스택 한 줄, 번호 |

- **줄 간격은 두 값뿐이다** — 본문 `1.65`, 라벨 `1.5`.
  🔴 **컴포넌트에 `leading-*`을 개별로 붙이지 말 것.** 실측에서 1.375·1.625·1.7 세 종이 섞여 있었다.
- **글자 크기 바닥 `max(1em, 13px)`.** 캡션이 12px 아래로 내려가면 읽는 사람이 포기한다.
- `font-variant-numeric: tabular-nums` — 기간·수치가 자리를 지킨다.

⛔ **스몰캡 폐기.** `all-small-caps`는 대문자 높이가 em의 약 72%라 11px 지정이 실제로는
**7.9px로 그려졌다.** 실측에서 텍스트 135개 중 52개가 실효 14px 미만이었다.
⛔ **이탤릭 금지.**

### 문단 폭 — 이 사이트에서 가장 자주 깨지던 규칙

```css
--measure:      34rem;  /* 544px — 17px 본문이 한 줄에 한글 약 32자 (권장 25~35자) */
--measure-lead: 44rem;  /* 704px — 큰 글자용. 34rem이면 20자에서 끊겨 토막난다 */
```

**`ch`가 아니라 `rem`인 것이 핵심이다.** `ch`는 글자 크기에 비례해 늘어나므로,
15px 문단과 20px 문단에 같은 `54ch`를 줘도 실제 폭이 90px 넘게 벌어진다.
그게 "문단마다 줄 끊기는 자리가 제각각"의 원인이었다 (전에는 46ch·48ch·52ch·54ch·
`max-w-xl`·`2xl`·`3xl`·68ch **여덟 값**이 섞여 있었다).

🔴 **컴포넌트에 `max-w-*`를 새로 붙이지 말 것.** `globals.css`가 한 번에 잡는다.
폭을 따로 잡아야 하는 리드 문단만 `max-w-(--measure-lead)`를 명시한다.

### 줄바꿈

- **`text-balance`는 제목에만.** 본문 문단을 여러 개 쌓을 때 balance를 주면 줄 수가 적은
  문단이 컨테이너보다 훨씬 좁게 그려져 **오히려 제일 들쭉날쭉해진다.** 쌓이는 문단은 `text-pretty`.
- `word-break: keep-all` — 한글이 단어 중간에서 안 끊긴다.
- 기술 스택 나열은 `stackLine()` — 이름 안 공백과 구분점 앞을 nbsp로 묶어
  제품명이 안 쪼개지고 `·`가 줄머리에 안 떨어지게 한다.
- 굵은 글씨가 앞 문장 꼬리에서 시작해 두 줄에 걸치면 안 된다 → 문맥이 바뀌는 자리에서 `block`.

---

## 4. 레이아웃

```
.wrap        max-width 1280px, padding-inline 1.5rem (md+ 3rem)  → 콘텐츠 1184px
12칼럼 그리드  라벨 col-span-4 (368px) · 본문 col-span-8 (776px), gap-10
섹션 간격     홈 py-20 md:py-28 · 상세 py-16 md:py-20
```

🔴 **상세 페이지는 본문 단이 하나다.** 모든 섹션이 라벨 `x=681` / 본문 `x=1089`에서 시작한다
(1440px 뷰포트 실측). 한 페이지 안에서 글이 시작하는 자리가 섹션마다 다르면
읽는 쪽은 그걸 편집 실수로 읽는다.

- Highlights의 이미지 열 **208px**은 임의값이 아니다 — 남는 글 폭이 정확히 `--measure`가
  되는 값이다. 이미지가 없는 항목도 그 자리를 비워 둔다.
- 12칼럼 × `gap-16`은 간격만 704px이라 768px에서 트랙이 붕괴한다 → 넓은 간격은 `lg`부터.
- grid 자식은 `min-w-0`. 기본 `min-width: auto`라 안쪽 내용이 트랙을 밀어낸다.

⛔ **같은 크기 3열 카드 그리드 금지.** 개발자 포트폴리오가 늘 쓰는 배열이라 아무것도 구분해주지 않는다.
목록은 카드가 아니라 **항목 나열**(`ProjectLedger`)이다.
⛔ **섹션 번호를 화면에 찍지 않는다.** `no`는 정렬 키다.

---

## 5. 질감·깊이

- **종이 결**: `body::before`에 SVG `feTurbulence`, `opacity 0.28`, `mix-blend-mode: multiply`.
  완전한 플랫은 무균실처럼 보이고, 과하면 무늬가 된다.
- **그림자는 배경 색조를 띤다**: `.plate` = `rgba(36, 30, 24, …)`. 순수 검정 그림자는 종이 위에서 때처럼 보인다.
- **괘선 1px, `--line` 하나.** 테두리·배경·그림자로 카드를 만들지 않는다.

---

## 6. 모션

| | 값 |
|---|---|
| 진입 리빌 | `reveal-up` 0.9s · `fade-up` 0.75s, `cubic-bezier(.16, 1, .3, 1)` |
| 링크 밑줄 | `scaleX` 0.4s `cubic-bezier(.65, 0, .35, 1)`, 오른쪽→왼쪽 origin 전환 |
| 스크롤 | Lenis |
| 접근성 | `prefers-reduced-motion`에서 **전부 0.01ms** |

- 진입 리빌은 **CSS 애니메이션**이다. JS 엔진의 rAF 경합에 영향받지 않고 마운트 시 항상 재생된다.
- ⚠️ **Lenis 때문에 `scrollIntoView`·`#anchor`가 안 먹는다.** 브라우저로 확인할 땐 실제 휠 스크롤을 쓸 것.
- ⚠️ 같은 이유로 `loading="lazy"` 판정이 흔들려 이미지는 `eager`다 (커밋 `88da47e`).

---

## 7. 성능 — 이 디자인이 지불하는 값

| | 값 |
|---|---|
| 렌더 블로킹 CSS | **gzip 11.1KB** (폰트 CSS 트림 전 62.2KB) |
| `@font-face` 선언 | **104개** (원본 368개) |
| 다음 depth 선반입 | 상세 이미지 153KB(DPR 1) / 282KB(DPR 2), idle · `fetchPriority=low` |

- 폰트 CSS는 `prebuild`의 `scripts/trim-font-css.mjs`가 `src/`의 글자를 훑어 자른다.
  원본은 `pretendard-all.css`(커밋), 생성물은 gitignore.
- 글자가 빠지면 그 글자만 시스템 폰트로 조용히 떨어지므로 `npm run check:fonts`가 대조한다 (CI 포함).
- 이미지: 원본 PNG → `prebuild`가 `r/<이름>-<너비>.webp` 생성. `WIDTHS`는 `next.config.ts`와 일치해야 한다.

---

## 8. 절대 제약 (`PRODUCT.md`)

1. **기관 내부 실데이터 비노출** — 근태 시트·관람객 전화번호는 구조만 재현한다.
2. **과장·미검증 클레임 금지** — 자가평가 레벨(n/5) 폐기, "무한스크롤" 문구 정정,
   AI 섹션의 "Claude의 평가" 서명 제거(자기 평가를 제3자 검증으로 포장한 것).
3. **WCAG AA.**

---

## 9. 페이지

| URL | 대상 | 비고 |
|---|---|---|
| `/` | 개발 직무 심사자 | Hero → ConceptStrip → Skills → AIPractice → ProjectLedger → Footer |
| `/projects/<slug>` | 상세 5건 | Why → Architecture → Stack → Highlights → Friction → Result → Next |
| `/field` | **기술영업·프리세일즈 심사자** | 지원 시 URL을 골라서 낸다. 랜딩에선 푸터 링크 한 줄로만 존재 |

`/field`는 사이트를 두 벌로 복제하지 않기 위한 선택이다. 복제하면 사실관계가 갈라져
한쪽만 낡고, 면접관이 두 링크를 다 보면 그게 더 위험하다. 콘텐츠 소스는 `projects.ts` 하나다.

---

## 10. 고치기 전에 읽을 것

- `PRODUCT.md` — 목적, 절대 제약, Evidence on Hand, "지어내면 안 되는 것"
- `AGENTS.md` — **Next.js 16은 학습 데이터와 다르다.** 코드 작성 전 `node_modules/next/dist/docs/` 확인
- `HANDOFF.md`
